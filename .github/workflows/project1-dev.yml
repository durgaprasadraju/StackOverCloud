name: Deploy Lambda Function to AWS

on:
  push:
    branches:
      - main  # Trigger deployment on push to the main branch
    paths:
      - 'project1/**'  # This will trigger the workflow only if files in project1 are changed  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Set up AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to Lambda (Project1)
        run: |
          zip -r function.zip project1/lambda/
          aws lambda update-function-code --function-name project1-main-lambda --zip-file fileb://function.zip